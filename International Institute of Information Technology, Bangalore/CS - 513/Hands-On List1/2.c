/*
 * C program to run a loop in the background and see details in the proc directory
 * Author: MT2019074
 *
 * 	./a.out &  (Sends process in the background)
 *'	 /proc/PID (Is the directory where information is stored)
 */
int main(){
	while(1);
	return 0;
}

